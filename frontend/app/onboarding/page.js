'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation';
const API='http://localhost:8000/api';
export default function Onboarding(){const r=useRouter();const[err,setErr]=useState('');async function submit(e){e.preventDefault();const token=localStorage.getItem('unum_token');const fd=new FormData(e.currentTarget);const body=Object.fromEntries(fd);const res=await fetch(API+'/stores',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+token},body:JSON.stringify(body)});const data=await res.json();if(!res.ok){setErr(data.detail||'Xatolik');return;}localStorage.setItem('unum_store_id',data.id);r.push('/dashboard');}
return <div className="authWrap"><div className="authCard"><div className="brand"><span className="brandMark">U</span> UNUM SAVDO</div><h1>Do‘konni yarating.</h1><p className="muted">Buni bir marta qilasiz. Keyin UNUM tayyor.</p><form className="form" onSubmit={submit}><div className="field"><label>Do‘kon nomi</label><input name="name" placeholder="Masalan: Baraka Market" required/></div><div className="field"><label>Do‘kon turi</label><select name="category"><option>Oziq-ovqat</option><option>Kiyim-kechak</option><option>Telefon aksessuarlari</option><option>Kosmetika</option><option>Qurilish mollari</option><option>Boshqa</option></select></div><div className="field"><label>Manzil (ixtiyoriy)</label><input name="address"/></div>{err&&<div className="error">{err}</div>}<button className="btn btnAccent">Do‘konni yaratish</button></form></div></div>}
